[package]
name = "mu-epub-render"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
license = "MIT"
description = "Render IR and layout engine for mu-epub"

[dependencies]
mu_epub = { path = "../.." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
